import 'package:flutter/material.dart';

class ManageCatalogue extends StatelessWidget {
  const ManageCatalogue({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("This is a page to manage store's catalogue")),
    );
  }
}